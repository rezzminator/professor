---
name: h:gh
description: Use for any GitHub operation (PRs, issues, releases, repo API); the CLI is installed and authenticated on this host.
---

# GH — GitHub CLI Bridge

Host fact, not project prose — a machine-scope command shared across every project on this host, never a per-repo copy. If `gh` turns out missing or unauthenticated, say so and stop rather than guessing at credentials.
