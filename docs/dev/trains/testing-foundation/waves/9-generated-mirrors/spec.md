# Wave 9 — generated Codex twins leave the templates; `pfm install` compiles them

**Law (the user's):** a generated mirror is never tracked beside its source. `templates/global/agents/{architect,reviewer,rr,scheduler,tracer}.toml` are compiled from the `.md` beside them by `pfm codex agents`; a stale compiler on any host can rewrite a tracked file (seen: a stray quote written into `rr.toml`). Independent of the testing waves; disjoint files.

## Target

- The five `.toml` twins are deleted from the repo and ignored.
- `pfm install` (global fan-out) compiles every `templates/global/agents/*.md` with the existing `codexgen` transformer into a pfm-owned generated directory under the pfm home (resolved through `paths`, recorded in the ownership ledger), and links each account's Codex agent registry (`~/.codex/agents/<name>.toml`) to that output. `pfm codex agents` recompiles into the same directory; nothing writes into the blueprint clone.
- Migration: an existing install whose links point at the deleted in-repo twins is re-pointed on its next `pfm install`; a link whose target is gone reports DANGLING by name in `pfm doctor` until then. `pfm uninstall` removes the generated directory and the links it owns.
- The roster gate (`scripts/check-agent-roster.mjs`, `dev.sh verify templates`) compiles in memory and asserts roster + protocol from the `.md` sources; it no longer reads tracked twins.
- OpenCode: confirm from the map whether any OpenCode output is tracked under `templates/**`; same law applies to whatever is found.

## Doors (filled by the tracer map before dispatch — closed-world, never "find the rest")

`tmp/walks/generated-twins.md` — every writer, reader, linker, gate, doc and test of the five twins: `pfm/internal/codexgen/globalagents.go` (tomlOutput beside the source), the installer's global fan-out and ledger, doctor rows, uninstall, `binwatch`/Stop-hook recompiles, `scripts/check-agent-roster.mjs`, `templates/refresh-map.json`, `.professor/manifest.json` hashes, `.gitignore`, the release rehearsal, e2e install tests, `CLAUDE.md` § three tiers ("compiles the Codex `.toml` twins beside their `.md`" — guarded, via /pcm), `templates/project/CLAUDE.md` twin if it restates it, `INSTALL.md`, `docs/{SETUP,BLUEPRINT,README}.md`.

## Ruled by the user: clean it all up

This repo's own generated mirrors leave version control too — `AGENTS.md`, `pfm/AGENTS.md`, `.codex/**` except its hand-written keepers (`config.toml` outside its generated fence, `rules`), and `.opencode/**` except hand-written keepers. They are produced by the existing writers (`pfm codex build`, `build-opencode.mjs generate`) at `pfm install` / `pfm init` time and by the Stop hook, and ignored by git. The tracer map lists each tracked generated file with its writer; the build spec carries: the ignore rules, the keeper list, what a fresh clone opened by Codex or OpenCode before any install sees (a bootstrap note in `README.md`/`INSTALL.md`, or a minimal hand-written `AGENTS.md` stub pointing at `CLAUDE.md` — decided from the map), the self-hosted manifest roster shrinking accordingly, `pfm codex check` and the OpenCode `check` still gating drift locally, and the plugin-scanner CI job generating before it scores.

## Proof

Regression tests watched red first: install on a clean home produces the generated twins and live links with no file written inside the clone; a second install is `changed=0`; an install over old in-repo links re-points them; uninstall leaves nothing. `dev.sh iso verify templates` + `iso test pfm` green; Tier B beat `O1` "codex global agents resolve after install" added with its landscape row.

## Map holes closed (read by the orchestrator, develop @ 53b05b9f)

- `infra/fence/release-rehearsal.sh`, `infra/demo/verify.sh`, `pfm/internal/installer/themes.go`, `pfm/internal/installer/global_fanout_test.go`: zero references to the twins, their directory, or `pfm codex agents` — not doors.
- Two more test doors: `pfm/internal/installer/migration_issue15_test.go:86-90` (a `~/.codex/agents/frr.toml` fixture whose header names `templates/global/agents/frr.md`) and `pfm/cmd/pfm/codex_agents_command_test.go:13-15,34,50` (asserts the compiled sibling `.toml` inside the clone and the `~/.codex/agents` link pointing at it) — both change with the output directory.
- Queue specs `2026-08-23-blueprint-compiler.md:457` and `2026-09-01-scaffold-and-own-update.md:121` mention the twins as unchanged background; reword the one line each when the wave lands.
- This repo's own mirror writers, confirmed: `.codex/skills/*/SKILL.md` carry the `Generated by pfm codex build` header (`codexgen/compiler.go:560,681`); `.codex/config.toml` is a hand-written keeper holding one generated `mcp_servers` fence (`codexgen/mcp.go:13-15`) — it stays tracked whole; `.opencode/{LICENSE,SECURITY.md,opencode.jsonc}` and `agent/`, `command/` are `build-opencode.mjs` outputs. 34 tracked mirror files in all.
- The eleven blanket-dispositioned project-tier files: zero twin references except `.professor/drift.md:104,315` (dated history, left as a record) and `.codex/skills/pcm/SKILL.md` (generated; follows its source).

## Build split

- **9a — engine (dev, fenced worktree `.worktrees/tf-mirrors` from develop):** `codexgen.RunGlobalAgents` writes to a pfm-owned generated directory resolved through `paths`; the `~/.codex/agents` links point there; migration re-points old in-clone links; doctor names a dangling link; uninstall removes the directory and its links; every test door above moves with it. No file under `templates/**`, `.claude/**`, or any `CLAUDE.md` is touched by 9a.
- **9b — tree cleanup (orchestrator via /pcm, after 9a is green):** delete the five tracked twins and ignore `templates/global/agents/*.toml`; untrack this repo's mirrors per § Ruled by the user with their ignore rules and keeper list; roster gate, manifest roster, scanner CI job, bootstrap note, and the doc lines listed under § Doors.
