// Package doctor is pfm's health pass: one row per probe over config,
// engines, MCP, the index, crumbs, hooks, the harness prompt, and the
// harvester runtime, with healthy, broken, and could-not-look kept distinct.
package doctor
