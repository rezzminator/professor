// Generated by .claude/scripts/build-opencode.mjs from .claude/scripts/build-opencode.mjs + .mcp.json; do not edit — edit the source, then re-run: node .claude/scripts/build-opencode.mjs
// Sources: this compiler (which owns $schema, permission — repo law pinned at
// the harness layer) plus the optional .mcp.json (absent when the repo ships
// no MCP servers; the pin survives without it). Every other key below survives
// regeneration. Hand edits to the owned keys are overwritten — that is the pin.
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "*": "allow",
    "external_directory": "allow",
    "doom_loop": "allow",
    "edit": {
      "**": "allow",
      "**/.claude/**": "deny",
      "AGENTS.md": "deny",
      "**/AGENTS.md": "deny",
      "CLAUDE.md": "deny",
      "**/CLAUDE.md": "deny",
      ".opencode/**": "deny"
    },
    "bash": {
      "*": "allow",
      "git commit*": "deny",
      "git push*": "deny",
      "git tag*": "deny",
      "gh release*": "deny"
    }
  }
}
