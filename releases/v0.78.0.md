# v0.78.0 — 2026-09-25

The chat and harvester MCP servers become one `professor` server and the harvester's tools are renamed for their intent, with no aliases: every permission rule, agent `tools:` list, prompt or script that names an `mcp__harvester__*` or `mcp__chat__*` tool, or an old harvester tool, stops matching the moment the update lands. The wave engine is retired for the flights family, the per-project `dev` and `qa` agents give way to a machine-global flights cast, and a new machine-wide `git-guard` hook lets only the `gitter` agent write git — the main chat included. `pfm update` runs the new binary's `pfm install --yes`, so the MCP re-registration, the new hooks and the global roster land on their own; restart every running chat afterwards, then run `pfm update check` in each adopted project and apply the template changes listed below. Follow `INSTALL.md` § Updating for the whole sequence; going back to v0.77.2 needs the repair line under the MCP bullet.

Work every `#### → For:` line below, not only `pfm update --to`: an install on v0.76.0 or older runs an update prompt that reads only this note and builds no checklist, so first read each skipped `releases/vX.Y.Z.md` after your installed version, oldest first, and work their `#### → For:` lines too — above all v0.77.0's before-update step: attach the detached source clone with `git -C "$HOME/.professor" switch -c professor`, or `pfm update` refuses with "source checkout is detached". In each adopted project, a `clean` `pfm update check` does not mean the retired surface is gone: `pfm update drop` only forgets a pin and leaves the local file live, so DELETE every retired file this note lists — the `wave/*` commands, `commands/pfm.md`, the `documenter*` and `km` files, the per-project `developer` and `qa` agents — before you drop it.

## Breaking

- pfm: professor MCP server — the `chat` and `harvester` MCP servers merge into one server keyed `professor`, registered on Claude, Codex and OpenCode as the stdio command `pfm mcp serve --stdio`, which forwards to the daemon and keeps each caller's identity. Claude and Codex now see `mcp__professor__harvester_read`, `mcp__professor__chat_inject` and so on (OpenCode: `professor_harvester_read`); `issue_servicedesk` becomes `servicedesk`. The daemon serves `/mcp/professor` and the family views `/mcp/professor/harvester` and `/mcp/professor/chat`; `/mcp/harvester` and `/mcp/chat` answer 404. `pfm mcp chat serve`, `pfm mcp harvester serve` and bare `pfm mcp` are removed. The update's `pfm install` removes pfm's own legacy `chat`/`harvester` entries by exact shape in every Claude registry, `~/.mcp.json`, each Codex `config.toml` and the OpenCode config, and keeps any entry you edited by hand. When one family fails to configure, the other still serves and the failed family's tools answer with an error naming the config path.
#### → For: every adopter · after update · running chats — restart every chat that was running during the update (or `/reload` it): it still holds the old `chat`/`harvester` registration, which the new binary no longer serves; a restarted chat lists `mcp__professor__*` tools.
#### → For: every adopter · after update · MCP registrations — run `pfm doctor`; a line `doctor: mcp client=… legacy=…` with `remediation=run pfm install --yes` names a legacy entry pfm left because you edited it — remove or rename that entry by hand, then `pfm doctor` shows no `legacy=` suffix.
#### → For: every adopter · per project · MCP tool names — replace every `mcp__harvester__*` and `mcp__chat__*` name in the project's `.claude/settings.json` permissions, agent `tools:` lists, commands and scripts with its `mcp__professor__harvester_*` / `mcp__professor__chat_*` form; `grep -rnE 'mcp__(harvester|chat)__' .claude CLAUDE.md` then prints nothing.
#### → For: adopters who roll back to v0.77.2 · after update · MCP registrations — v0.78.0's `pfm update` refuses a downgrade, so check out `v0.77.2` in the source clone, rebuild pfm as `INSTALL.md` § 2 shows and run `pfm install --yes` from the clone; v0.77.2's own install re-registers `chat`/`harvester` but leaves `professor` behind: delete the `"professor"` key under `"mcp"` in `~/.config/opencode/opencode.jsonc`, run `claude mcp remove professor -s user` once per Claude account that still lists it, and `grep -n '"professor"' ~/.claude.json ~/.mcp.json ~/.config/opencode/opencode.jsonc` prints nothing.
- pfm: harvester tools — the harvester serves four tools named for their intent: `read` (replaces `readPage`, `parseLocalDocuments` and `readWork`; takes `urls`, `files` and `publications` in one call), `download_file` (replaces `download`), `search_literature` (replaces `findWorks`; `kind`→`type`, `count`→`limit`) and `search_web` (replaces `webSearch`). The archive member readers, `searchCache`, `fetchImage` and the `fetch` prompt are gone. Result fields are renamed: `size_only`→`include_content`, `ocr_lang`→`ocr_language`, `method`/`route`→`via`, `partial`→a `gaps` list, `cache_status`→`cached`, `http_status`→`status`. The CLI follows: `pfm harvest download-file`, a new `pfm harvest search`, `--include-content=false`, `--ocr-language`, and `--json` uses the MCP field names. No aliases. The remote gateway serves the same tools under their `harvester_` names; `download_file` there answers a signed ten-minute link, never a server path; `harvest.maxDownloadBytes` and `harvest.maxResourceBytes` are new config keys.
#### → For: adopters who call the harvester from their own prompts, scripts or remote MCP clients · after update · harvester tool names — rename each old tool, field and CLI flag to its new name above; a call to an old name now fails as an unknown tool.
- Global: flights replaces wave — `/flights:spec` (maps the area and grills the user) → `flights-speccer` (task files) → `/flights:orchestrate-nested`, `/flights:orchestrate-live` or `/flights:orchestrate-cross-harness` → `flights-orchestrator` → one fresh `flights-mechanical-executor` or `flights-smart-executor` per task file → `flights-lander` (checks, one whole-diff review, adversarial tests) → `/flights:audit`. `/wave:builder`, `/wave:ccc`, `/wave:live`, `/wave:orchestrator`, `/wave:refine`, `/wave:walker`, `/wave:walker-invariants`, the `scheduler` and `architect` agents and the wave-builder skill are removed; the update's `pfm install` retires their dangling links and Codex roles. Reds carry their cause, a second red diagnoses first, a third is BLOCKED; executors run only their affected tests.
#### → For: every adopter · after update · wave commands — start new work with `/flights:spec` where you typed `/wave:refine` or `/wave:builder`; `ls ~/.claude/commands` no longer lists `wave`.
#### → For: source installs that built the wave-walker engine · after update · `~/.professor/engines` — optional cleanup: nothing reads the old build tree any more and the clone now ignores it, so `rm -rf "$HOME/.professor/engines"`; `test -e "$HOME/.professor/engines" || echo gone` then prints `gone`.
- Project: flights cast replaces per-project developer and qa — `agents/per-project/developer.md` and `agents/per-project/qa.md` are gone upstream; the machine-global flights cast does their work and reads the project's test law from the new template `commands/per-project/testing-manual.md` (`/per-project:testing-manual` in the template tree), installed as `.claude/commands/{project}-testing-manual.md` (`testing-manual.md` for a single-project install). The project wave surface goes with them: `commands/wave/live.md`, `commands/wave/orchestrator.md`, `commands/wave/walker.md`, `commands/wave/walker-invariants.md`, `codex/skills/wave-builder/SKILL.md`, `docs-commands/wave/references/debug-discipline.md`, `docs-commands/wave/references/fix-core.md`, `docs-commands/build/references/build-reference.md` and `docs-commands/git/references/gitter-phase-wave.md`.
#### → For: every adopter · per project · `pfm update check` — adopt the `NEW` template `project/commands/per-project/testing-manual.md` as `.claude/commands/{project}-testing-manual.md` for each project in the roster (`.claude/commands/testing-manual.md` for a single-project install), filled with that project's own test commands, and map it with `pfm update pin --template project/commands/per-project/testing-manual.md <local>`; for each `GONE-UPSTREAM` file above delete the local copy first (keep a customized one only if you still use it) and then run `pfm update drop <local>`; `pfm update check` then lists none of them and `ls .claude/commands/wave .claude/agents/developer.md .claude/agents/qa.md` finds none of them.
- pfm: guard hooks — `pfm install` wires `pfm internal git-guard` as a machine-wide `PreToolUse` Bash hook in every Claude account: every caller except an agent of type `gitter` — the main chat included — is denied git's shared-state writes (commit, merge, rebase, reset, push, fetch, pull, switch, branch/tag writes, add/rm/clean, whole-tree checkout or restore, whole-repo stash, worktree add/remove, config writes and more); reads and a path-scoped `stash push -- <path>` stay allowed. The deny message tells the caller to spawn `gitter`, which now ships machine-global. There is no opt-out key. `pfm internal orchestrator-wait`, attached only in the orchestrator agents' frontmatter, denies a Bash call that only waits (`sleep`, bare `echo`, `true`). (cost)
#### → For: every adopter · after update · git writes — route commits, merges, worktrees and pushes through `Agent(subagent_type: "gitter")`; a project that overrides `gitter` keeps its own by name. A `git commit` from the main chat now answers `git-guard blocked`.

## Added

- Global: general orchestration family — `general-orchestrator` runs a batch of clear tasks, one `general-mechanical-executor` or `general-smart-executor` per task, and returns once; the fleet prompt's orchestration ladder routes solution-in-hand batches here and open designs to flights.
- Global: tracer, mapper and collector — `tracer` answers numbered code questions, `mapper` maps one target's whole area, `collector` copies exact code text; all three run on the new `codeprobe` skill.
- Global: gitter — the git-writing role ships machine-global; its Merge phase refuses while a named reviewer report holds an unresolved finding.
- Global: quality commands — `/quality:llm-codebase` (tree layout and its mechanical gates) and `/quality:integration-suite` (every test tier's validity and the live tier's lanes) replace the `architecture-design` skill.
#### → For: every adopter · after update · `architecture-design` skill link — `pfm install` retires orphaned agent and command links but not skill links: remove the dangling `skills/architecture-design` link from each Claude account's config directory (for the default account `find ~/.claude/skills -maxdepth 1 -xtype l -name architecture-design -delete`); `ls ~/.claude/skills` then lists no `architecture-design`.
- pfm: rr save directory and agent variants — `templates/global/agents/variants.json` declares agents rendered from an original (`super-rr`, `heavy-rr`, `flights-smart-executor`, `general-smart-executor`); `pfm install` writes them to pfm's own generated directory, links them into every Claude account and compiles their Codex roles, and refuses a malformed declaration before any write. A new `SubagentStart` hook `pfm internal rr-dir` hands the rr family its save directory (`RR-DIR:` the nearest ancestor holding `.professor/`).
- pfm: callmeter — a hook recorder registered async on seven events writes every tool call, request and agent, with the account that ran it, to `~/.local/state/pfm/callmeter.db`; it never sits in front of a call.
- pfm: OpenCode — `pfm opencode build|check|doctor` compiles native OpenCode mirrors (agents, commands, skills, `opencode.jsonc`); a compiled role reaches exactly the MCP and builtin tools its Claude source lists, and a bad source is skipped by name while every healthy output is written. A running OpenCode chat is a live fleet seat: listed, named, read by `chat status`, and killed with `/exit`.
- pfm: headless — `pfm headless exec --engine opencode|ox` drives OpenCode's run protocol with `--tools`, `--setting-sources`, `--strict-mcp-config`, `--no-session-persistence` and `--sealed`; Claude runs sum `modelUsage` and the receipt carries the run error.
- pfm: config — new optional keys, each also settable per account: `claude.theme` (merged into every Claude launch's `--settings`), `claude.nativeCursor` (`CLAUDE_CODE_NATIVE_CURSOR=1`), `claude.maxSubagentSpawnDepth` (default 8) and `claude.maxConcurrentSubagents` (Claude Code's own ceilings when unset). No key is removed; an unknown key still fails the config load, which `pfm config validate` names. (opt-in)
- pfm: themes — `pfm install` ships bundled per-account palettes as overlays on Tokyo Night, with livelier input bars; Claude panes launch with `CLAUDE_CODE_TMUX_TRUECOLOR=1` so tmux stops quantising theme colours to 256.
- pfm: activity log — pfm writes a JSON-lines activity log to `~/.local/state/pfm/log/pfm.jsonl` from every door (CLI, MCP, HTTP, runner, tmux, hooks, db, installer, state), credentials scrubbed. `pfm install` adds `"log": {"level": "info", "keepDays": 30}` to `pfm.config.json` when no `log` block exists; `log.level` (`debug`, `info`, `warn`, `off`), `log.components`, `log.keepFiles`, `log.maxMB`, `log.keepDays` and `PFM_LOG_LEVEL` / `PFM_LOG_COMPONENTS` / `PFM_LOG=stderr` control it. (cost)
- pfm: harvester sites and formats — site extractors load whole threads from Reddit, Hacker News, GitHub issues, pull requests and Discussions, Stack Exchange, Discourse, GitLab, dev.to, Lemmy, Lobsters, Mastodon, Bluesky, Substack, Slashdot, YouTube (transcript and chapters), Steam, Notion, Product Hunt, IMDb, Quora and Booking, setting each page's stated count against what loaded and naming the rest; documents are routed by their bytes and parsed in every measured format, OCR and Office formats included; a file the direct, Chrome and Wayback rungs cannot get is downloaded by the browser; callers may pass request headers.

## Changed

- Project: /pfm becomes /pcm — `commands/pfm.md` is renamed `commands/pcm.md` (`/pcm`, the Professor Change Manager) with its audit scopes inline, so `commands/pfm/references/audit-scopes.md` is gone; `/pfm` now ships machine-global as the `pfm` CLI guide.
#### → For: every adopter · per project · `commands/pcm.md` — take the new `commands/pcm.md` from `pfm update check` (carry your customizations of the old `commands/pfm.md` into it), pin it, then delete the local `commands/pfm.md` and `commands/pfm/references/audit-scopes.md` and `pfm update drop` each; `/pcm` then opens the change manager and `/pfm` the CLI guide.
- Project: hooks and scripts — `settings.json` unwires `scripts/notify.sh`, `scripts/filter-test-output.sh` and `scripts/km-guard.sh`, and all three scripts are gone upstream, with the unused `scripts/baseline-sync.sh`. Guard markers from `scripts/pfm-guard.sh`, `scripts/guard-stamp.sh` and `scripts/codex-sync.sh` move to `/tmp/{project}/guard/`, and scratch lives in `/tmp/{project}/{purpose}/`, never the working tree; the codex-sync Stop hook finds a mise-installed node, warns instead of blocking, and reports a failed repair once through the hook's JSON reason. `scripts/build-codex.mjs`, `scripts/dev.sh`, `scripts/drain-wait.sh`, `scripts/alloc-ports.sh` (a generic `PROJECTS` roster with one port each plus `EXTRA_PORTS`) and `scripts/worktree.sh` follow.
#### → For: every adopter · per project · `.claude/settings.json` — hand-apply the `settings.json` diff first, so no hook still calls `notify.sh`, `filter-test-output.sh` or `km-guard.sh`, then delete those scripts and `baseline-sync.sh` and `pfm update drop` each; `grep -nE 'notify.sh|filter-test-output.sh|km-guard.sh' .claude/settings.json` prints nothing.
- Project: CLAUDE.md and prompts — `CLAUDE.md` and `per-project/CLAUDE.md` route framework changes to `/pcm` and work to `/flights:spec`, drop the `/documenter` and `/km` routes, the `ROOT/tmp/` scratch rule and `.professor/drift.md` (a customization's commit message is its record). The rest re-point wave-era names at flights and follow the generic roster: `agents/gitter.md`, `docs-commands/git/references/gitter-phase-docs.md`, `docs-commands/git/references/gitter-phase-merge.md`, `docs-commands/git/references/gitter-phase-push.md`, `docs-commands/git/references/gitter-phase-setup.md`, `commands/audit/code-hygiene.md`, `commands/audit/security.md`, `commands/dev.md`, `commands/marketer.md`, `commands/mentor.md`, `commands/officer.md`, `commands/rnd.md`, `docs-agents/_index.md`, `docs-agents/standards.md`, `docs-commands/build/references/qa-commons.md`, `epics/TEMPLATE.md`, `rumdl-policy.toml`, `skills/legal/references/dpa-drafting-guide.md`, `skills/legal/references/privacy-compliance-dpa.md`, `skills/legal/references/skill-optimizer.md`, `skills/legal/references/vendor-due-diligence.md`, `skills/sources.json`, and the Codex keepers `codex/README.md`, `codex/config.toml`, `codex/rules/repo-law.rules` and `codex/skills/chat/SKILL.md`.
#### → For: every adopter · per project · `pfm update check` — hand-apply each `UPDATED` diff above, keeping your project's values, and `pfm update pin <local>`; then rebuild any engine mirror the project opted into (`pfm codex build . && pfm codex check .`, `pfm opencode build . && pfm opencode doctor .`), and `pfm update check` reports no `UPDATED` row.
- Global: rr family — `rr` loops rabbit-hole rounds (at most four, four batched `sub-rr` diggers each) until its goal is met and saves to the directory the `rr-dir` hook names; `super-rr` and `heavy-rr` are stronger variants, `collector-rr` fetches named web sources verbatim, and the diggers wait by ending their message, never by spawning idle agents. `rr-super` is retired.
- Global: deep-rr — the research workflow calls the renamed harvester tools and never counts an unverified claim as verified.
- Global: tokens — the token audit script is `token-audit.mjs` (was `token-ledger.mjs`), fixture-tested; polls are counted past harness attachments and unledgered flight transcripts are priced.
- pfm: Codex — the fleet prompt reaches Codex through a pfm-owned `developer_instructions` fence in each Codex home's `config.toml` (a hand-written value outside the fence is kept and reported) instead of the truncated SessionStart appendix hook, which install removes; every compiled role carries the fleet prompt; global roles are written as regular files (Codex rejects a symlinked role) with `sandbox_mode` and their model pin; `/code-review` mirrors to a scoped `codex review`; registered agent roles launch natively; long command waits honour `code_mode` yield and background-terminal timeout defaults.
- pfm: fleet prompt — the prompt is composed per engine from a shared head and tail at install time (Claude `--system-prompt-file`, Codex `developer_instructions`, OpenCode `instructions`); it carries the orchestration ladder, the deletion and absence-test rules, and diagnose-first escalation on reds; `pfm doctor` names an embed that differs from the clone.
- pfm: install — `pfm install` owns Claude Code version retention, registers MCP in every registry a launched `claude` reads, reaches the implicit seat on every fan-out and retirement, removes owned hooks from dropped seats and orphaned hooks a rollback strands, keeps one copy of each owned hook, strips any `pfm` hook subcommand the binary no longer implements, registers the VS Code extension in each product's index, retires orphaned global agent and command links and Codex roles, and compiles global Codex twins into a pfm-owned generated directory, never the clone.
- pfm: update — `pfm doctor` separates failures from warnings and `pfm update` gates only on failures; an update run by this binary passes the migrated config to doctor, restores renamed config files, hooks and every MCP registration on rollback, and refuses a downgrade it cannot rule out; `pfm ls` never hides a known update or check failure. The update chat's prompt now honours `#### → Stop:` lines and merges actions by timing — it ships in the binary, so the update into this release still runs v0.77.2's prompt and the next update uses the new one.
#### → For: source installs on v0.76.0 or older · before update · source clone checkout — attach the detached clone once with `git -C "$HOME/.professor" switch -c professor` (v0.77.0's step, which the old update prompt never shows), then run `pfm update`; `git -C "$HOME/.professor" branch --show-current` prints a branch name.
- pfm: statusline — the status line renders in Go (`pfm internal statusline`; the Python renderer is retired), recomputes occupancy from the transcript, follows Claude Code's own `prompt_cache` window, and wears the effort level as a vehicle glyph; `pfm install` wires a sub-agent status line with a per-row context gauge, model and effort, tool count, errors and cache hit.
- pfm: name-sync — `pfm name-sync` previews by default and changes names only with `--apply`; the installed timer and launch agent carry `--apply`.
#### → For: adopters who run `pfm name-sync` by hand · after update · `pfm name-sync` — add `--apply` to perform the rename; without it the command only prints the preview.
- Repo: gates and CI — pinned lint, format, coverage, clone and architecture gates for pfm in `dev.sh` and CI; a skipped test not named in `known-skips.tsv` fails the gate; the plugin scanner's last points cleared.
- Repo: fences, lanes and demo — the `infra/fence` isolation fence, `dev.sh iso run` and `dev.sh iso sim` (a fresh Linux machine with Chrome), live-lane harness and mock engine, and a re-runnable live-demo fence; `/readme-gif` retired.
- Repo: docs — README, BLUEPRINT, SETUP and PLACEHOLDERS describe what ships at this tag; docs name the four harvester tools.
- Repo: release process — releases are prepared by `/pfm:release prepare` and published by `/pfm:release publish`, run by the `releaser` and `changelogger` agents and gated by `scripts/release-check.mjs`; this repo's own `/pcm`, `/pfm-testing-manual`, `dev` and `qa` changes stay local.
- Repo: generated mirrors — this repository no longer tracks `AGENTS.md`, `.codex/agents`, `.codex/skills` or `.opencode/**`; a fresh clone generates them before it checks.
- Repo: Go module — the Go module and every internal import move to the public Professor namespace.

## Fixed

- Global: agent and command frontmatter — five frontmatters parse as strict YAML; agent model pins are bare aliases and the roster check fails an inline comment.
- pfm: chat and MCP — inject reads Codex's idle state and the composer correctly, returns focus from the agents panel, and its waiters name what they wait for; `/reload` reaches every seat, `--new` keeps the chat's name; `chat kill` confirms the pane is gone and names what it did; `chat save` exports a Codex rollout; a Codex reply is read once; `chat_open` opens detached; a failed look is an error, never an absence; a backgrounded Claude chat is one chat; worktree chats file under their repo; spawns drop inherited `CLAUDE_PROJECT_DIR` and `CLAUDE_CODE_SSE_PORT`; a long launch command runs through a one-shot script; the MCP proxy keeps its session through a rejected probe; a daemon restart drains in-flight calls in about 0.3 s.
- pfm: harvester — bot walls, paywalls, login walls and consent dialogs are named, never stored as success; a URL's userinfo never reaches an answer; the browser runs headless only behind a pinned loopback proxy; rate limits wait once for `Retry-After`; `download_file` reports `isError` when every item failed; pending OAuth consents are bounded and revoke requires client auth; the HTML converter keeps headings, tables, code blocks and footnotes.
- pfm: doctor — rows for the service manager, crumbs, baseline, embed, Codex `developer_instructions`, spawn audit, MCP serve and the daemon tell "could not ask" from "absent" and name what they found; the harness prompt is captured from a throwaway config dir.
- pfm: init — a second `pfm init` without `--force` refuses instead of zeroing the baseline; the blueprint SHA resolves from a linked worktree.
- pfm: launcher — a relative `claude.binary` resolves on `PATH` or fails loudly; the default `claude` name keeps its native fallback.

## Removed

- Project: retired project surface — `/documenter`, `/documenter:archive`, `/km`, `/pm`, `/audit:ai-output` and `/git` leave the project scaffold with the agents `mono-architect`, `mono-documenter`, `mono-planner`, `rndier`, `role-wrapper` and `qa-wrapper` and every project workflow script: `agents/mono-architect.md`, `agents/mono-documenter.md`, `agents/mono-planner.md`, `agents/rndier.md`, `agents/role-wrapper.md`, `agents/qa-wrapper.md`, `commands/documenter.md`, `commands/documenter/_base.txt`, `commands/documenter/_epic-consolidation-contract.txt`, `commands/documenter/archive.md`, `commands/km.md`, `commands/pm.md`, `commands/audit/ai-output.md`, `docs-commands/documenter/references/doc-approval.md`, `docs-commands/documenter/references/sync-rules.md`, `docs-commands/documenter/references/scopes/epic.md`, `docs-commands/documenter/references/scopes/root-api.md`, `docs-commands/documenter/references/scopes/root-arch.md`, `docs-commands/documenter/references/scopes/root-db.md`, `docs-commands/documenter/references/scopes/root-features.md`, `docs-commands/documenter/references/scopes/root-map.md`, `docs-commands/documenter/references/scopes/{project}.md`, `workflows/audit-ai-output-sessions.js` and `workflows/documenter-fanout.js`. Doc consolidation is the main session's work under `/quality:doc`.
#### → For: every adopter · per project · `pfm update check` — delete each `GONE-UPSTREAM` file above unless you still use your local copy, run `pfm update drop <local>` for each, and remove any `/documenter` or `/km` route left in the project's `CLAUDE.md`; `grep -nE '/documenter|/km' CLAUDE.md` prints nothing.
- pfm: dream — the Dream memory organ and `pfm dream` are removed; `pfm install` keeps stripping legacy dream hooks from Claude and Codex settings.
